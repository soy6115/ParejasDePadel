<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Paddel" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/amarillo</item>
        <item name="colorAccent">@color/white</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="CajaCrearJugador" parent="android:Theme.Dialog">
        <item name="android:windowBackground">@color/transparente</item>
    </style>

    <style name="CajaEliminar" parent="android:Theme.Dialog">
        <item name="android:windowBackground">@color/azulOscuro</item>
    </style>

    <style name="Theme.ParejasDePadel.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>